# 05_specialized

String algorithms, range queries, bitsets và probabilistic/amortized ideas.

Bổ sung suffix structures và sparse table cho substring indexing và static range queries.

`06_probabilistic_data_structures.md` cover Bloom Filter, Count-Min Sketch, HyperLogLog và reservoir sampling.
