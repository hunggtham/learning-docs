10|||  - 💡 *Mẹo ghi nhớ*: UNION (hợp), INTERSECT (giao), EXCEPT (trừ). ALL nghĩa là lấy tất cả (kể cả trùng lặp).
15|||  - 💡 *Mẹo ghi nhớ*: Trigger giống như "cò súng", khi có sự kiện (bóp cò) thì sẽ tự động bắn (thực thi).
21|||  - 💡 *Mẹo ghi nhớ*: SAM MC (Sum, Avg, Max, Min, Count).
29|||  - 💡 *Mẹo ghi nhớ*: 1 Byte = char/boolean, 4 Bytes = int, 8 Bytes = long.
38|||  - 💡 *Mẹo ghi nhớ*: Chỉ dùng `A-Z, a-z, 0-9, _`. Số không đi đầu. Không dùng từ khóa.
43|||  - 💡 *Mẹo ghi nhớ*: "Garbage" (rác) -> Dọn dẹp bộ nhớ không dùng đến.
53|||  - 💡 *Mẹo ghi nhớ*: Prefix (++a) = Làm trước. Postfix (a++) = Làm sau.
62|||  - 💡 *Mẹo ghi nhớ*: AND (&) khắt khe (đều phải 1). OR (|) dễ dãi (1 cái là đủ). XOR (^) thích sự khác biệt.
69|||  - 💡 *Mẹo ghi nhớ*: Tương tự như toán tử bit nhưng áp dụng cho giá trị đúng/sai (true/false).
75|||  - 💡 *Mẹo ghi nhớ*: Dấu `?` là hỏi xem điều kiện đúng không, nếu đúng lấy cái trước `:`, sai lấy cái sau `:`.
81|||  - 💡 *Mẹo ghi nhớ*: Dấu ngoặc () luôn cao nhất. Đơn, Số, Dịch, Quan, Bit, Logic, Điều, Gán.
89|||  - 💡 *Mẹo ghi nhớ*: d = decimal (số thập phân), c = character (ký tự), s = string (chuỗi).
94|||  - 💡 *Mẹo ghi nhớ*: 'f' trong printf là 'format' (định dạng).
100|||  - 💡 *Mẹo ghi nhớ*: 'ln' trong println là 'line new' (xuống dòng mới).
106|||  - 💡 *Mẹo ghi nhớ*: Nếu (if) đúng thì làm, nếu không (else) thì làm cái khác.
112|||  - 💡 *Mẹo ghi nhớ*: Đừng quên `break`, nếu không nó sẽ trôi xuống tận dưới cùng.
117|||  - 💡 *Mẹo ghi nhớ*: Dùng khi biết trước số lần lặp.
122|||  - 💡 *Mẹo ghi nhớ*: Kiểm tra điều kiện trước, làm sau. Có thể không chạy lần nào nếu điều kiện sai ngay từ đầu.
127|||  - 💡 *Mẹo ghi nhớ*: Làm (do) trước, hỏi (while) sau. Chắc chắn chạy ít nhất 1 lần.
134|||  - 💡 *Mẹo ghi nhớ*: Mảng (Array) lưu các giá trị cùng kiểu, Cấu trúc (Struct) lưu các giá trị khác kiểu.
139|||  - 💡 *Mẹo ghi nhớ*: Chỉ số mảng luôn bắt đầu từ 0.
144|||  - 💡 *Mẹo ghi nhớ*: `[hàng][cột]` (Row x Column).
150|||  - 💡 *Mẹo ghi nhớ*: Độ dài mảng phải lớn hơn số ký tự của chuỗi ít nhất 1 (để chứa `\0`).
157|||  - 💡 *Mẹo ghi nhớ*: `&` là địa chỉ (Address), `*` là giá trị (Value).
163|||  - 💡 *Mẹo ghi nhớ*: `a[i]` hoàn toàn tương đương với `*(a + i)`.
171|||  - 💡 *Mẹo ghi nhớ*: List dùng `[]` và linh hoạt. Tuple dùng `()` và cố định (bất biến).
177|||  - 💡 *Mẹo ghi nhớ*: Python List giống như một cái túi thần kỳ, có thể bỏ bất cứ thứ gì vào.
183|||  - 💡 *Mẹo ghi nhớ*: Key-Value (Khóa-Giá trị). Dùng `{}` giống như một từ điển thực sự (tra từ -> ra nghĩa).
190|||  - 💡 *Mẹo ghi nhớ*: `range(start, stop, step)`. Bao gồm `start`, nhưng **không** bao gồm `stop`.
199|||  - 💡 *Mẹo ghi nhớ*: `[start : stop : step]`. Giống range, không bao gồm `stop`.
209|||  - 💡 *Mẹo ghi nhớ*: `sep` = phân cách giữa các đối số, `end` = ký tự kết thúc (mặc định là xuống dòng `\n`).
215|||  - 💡 *Mẹo ghi nhớ*: `split()` để cắt khoảng trắng, `map()` để ép tất cả sang kiểu nguyên `int`.
222|||  - 💡 *Mẹo ghi nhớ*: `for item in tập_hợp`. Lặp qua từng phần tử.
228|||  - 💡 *Mẹo ghi nhớ*: Python hỗ trợ cả lập trình thủ tục (như C) và hướng đối tượng (OOP). Tham số đầu tiên của hàm trong lớp luôn là `self`.
237|||  - 💡 *Mẹo ghi nhớ*: JS = Client Web, PHP = Server, Python = Thông dịch, Shell = Lệnh HĐH.
246|||  - 💡 *Mẹo ghi nhớ*: Built-in = Không cần cài, External = Cần pip/npm/v.v.
256|||  - 💡 *Mẹo ghi nhớ*: UNIX = C + Cây (Tree) + Đa nhiệm/Đa người dùng.
260|||  - 💡 *Mẹo ghi nhớ*: Kernel là "Trái tim" làm mọi công việc cốt lõi phần cứng.
264|||  - 💡 *Mẹo ghi nhớ*: Shell là "Vỏ bọc" giao tiếp với Kernel.
274|||  - 💡 *Mẹo ghi nhớ*: fork (nhân bản, nĩa), chmod (change mode), chown (change owner).
281|||  - 💡 *Mẹo ghi nhớ*: First = Nhanh nhất. Best = Tiết kiệm nhất. Worst = Còn lại khoảng trống lớn nhất.
288|||  - 💡 *Mẹo ghi nhớ*: Thrashing = Kẹt xe bộ nhớ (quá tải).
292|||  - 💡 *Mẹo ghi nhớ*: Nộp -> Nhận -> Chờ chạy -> Chạy -> (Tạm dừng nếu cần) -> Xong.
301|||  - 💡 *Mẹo ghi nhớ*: Công thức = `(Đợi + Chạy) / Chạy`. Số càng lớn càng ưu tiên. Giải quyết nhược điểm của SJF (tiến trình dài bị bỏ đói).
310|||  - 💡 *Mẹo ghi nhớ*: IPv4 = 32 bit (dấu `.`). IPv6 = 128 bit (dấu `:`).
317|||  - 💡 *Mẹo ghi nhớ*: Data Link = Frame/MAC. Network = IP/Routing. Transport = TCP/UDP/Port. Session = Dialog/Token.
330|||  - 💡 *Mẹo ghi nhớ*: TCP = Cẩn thận, chậm mà chắc. UDP = Nhanh, mất gói cũng không sao (Video call, Game).
343|||  - 💡 *Mẹo ghi nhớ*: Batch là tự động chạy ngầm khối lượng lớn, nên không được chết giữa chừng.
363|||  - 💡 *Mẹo ghi nhớ*: Java dùng Unicode nên `char` là 2 Bytes. Có thêm kiểu `byte` (1 Byte).
376|||  - 💡 *Mẹo ghi nhớ*: Trong C, 0 là Sai, mọi số khác 0 đều được coi là Đúng (Thường dùng 1).
391|||  - 💡 *Mẹo ghi nhớ*: "Scan" là quét (đọc vào), luôn nhớ phải có dấu `&` để chỉ đường cho dữ liệu đi vào bộ nhớ.
399|||  - 💡 *Mẹo ghi nhớ*: o = octal, x = hex, u = unsigned, p = pointer.
429|||  - 💡 *Mẹo ghi nhớ*: Xếp hạng hoặc các điều kiện loại trừ lẫn nhau thì dùng `else if`.
435|||  - 💡 *Mẹo ghi nhớ*: `switch` thích sự chính xác tuyệt đối (giá trị cụ thể), không thích sự so sánh lớn/nhỏ.
454|||  - 💡 *Mẹo ghi nhớ*: `break` = Phá vỡ (thoát ra). `continue` = Tiếp tục (bước tiếp).
461|||  - 💡 *Mẹo ghi nhớ*: Mảng trong C/Java đếm từ 0.
468|||  - 💡 *Mẹo ghi nhớ*: Array 2D giống như ma trận Toán học. `i` là hàng (bên ngoài), `j` là cột (bên trong).
477|||  - 💡 *Mẹo ghi nhớ*: C/Java không tự làm sạch bộ nhớ trừ khi bạn khởi tạo ít nhất 1 phần tử.
487|||  - 💡 *Mẹo ghi nhớ*: Con trỏ giống như tấm bản đồ (chỉ chứa địa chỉ), dùng `*` để đi đến đích và lấy kho báu (giá trị).
499|||  - 💡 *Mẹo ghi nhớ*: Python yêu cầu thụt lề (thường là 4 spaces) vô cùng khắt khe. Sai thụt lề = Lỗi (IndentationError).
516|||  - 💡 *Mẹo ghi nhớ*: C = Pointer, ALGOL = Algorithm, COBOL = Business, FORTRAN = Formula Translation.
522|||  - 💡 *Mẹo ghi nhớ*: Java = Đa luồng/Độc lập HĐH, Smalltalk = Thuần OOP/GUI đầu tiên.
530|||  - 💡 *Mẹo ghi nhớ*: AI = LISP & PROLOG. XML = Tag tự định nghĩa. Haskell = Hàm thuần túy.
539|||  - 💡 *Mẹo ghi nhớ*: io = Input/Output, lib = Library (chung chung như cấp phát bộ nhớ), str = String.
545|||  - 💡 *Mẹo ghi nhớ*: Exception = Bắt lỗi chủ động.
564|||  - 💡 *Mẹo ghi nhớ*: 제어 (Điều khiển) = Giám sát, Công việc, Dữ liệu. 처리 (Xử lý) = Dịch ngôn ngữ, Tiện ích.
602|||  - 💡 *Mẹo ghi nhớ*: Page = Kích thước cố định (Sinh ra rác bên trong). Segment = Kích thước logic (Sinh ra rác bên ngoài).
676|||  - 💡 *Mẹo ghi nhớ*: ARP = "A"ddress Resolution (Tìm MAC từ IP). RARP = "R"everse (Ngược lại).
693|||  - 💡 *Mẹo ghi nhớ*: Kỹ nghệ phần mềm = Khoa học + Quản lý + Tiết kiệm tiền & thời gian.
719|||**💡 Mẹo ghi nhớ (Mnemonics):**
738|||**💡 Mẹo ghi nhớ (Mnemonics):**
757|||**💡 Mẹo ghi nhớ (Mnemonics):**
776|||**💡 Mẹo ghi nhớ (Mnemonics):**
799|||**💡 Mẹo ghi nhớ (Mnemonics):**
814|||**💡 Mẹo ghi nhớ (Mnemonics):**
832|||**💡 Mẹo ghi nhớ (Mnemonics):**
858|||**💡 Mẹo ghi nhớ (Mnemonics):**
876|||**💡 Mẹo ghi nhớ (Mnemonics):**
923|||**💡 Mẹo ghi nhớ (Mnemonics):**
942|||**💡 Mẹo ghi nhớ (Mnemonics):**
963|||**💡 Mẹo ghi nhớ (Mnemonics):**
980|||**💡 Mẹo ghi nhớ (Mnemonics):**
1008|||**💡 Mẹo ghi nhớ (Mnemonics):**
1026|||**💡 Mẹo ghi nhớ (Mnemonics):**
1043|||**💡 Mẹo ghi nhớ (Mnemonics):**
1061|||**💡 Mẹo ghi nhớ (Mnemonics):**
1089|||**💡 Mẹo ghi nhớ (Mnemonics):**
1108|||**💡 Mẹo ghi nhớ (Mnemonics):**
1150|||**💡 Mẹo ghi nhớ (Mnemonics):**
1176|||**💡 Mẹo ghi nhớ (Mnemonics):**
1195|||**💡 Mẹo ghi nhớ (Mnemonics):**
1230|||**💡 Mẹo ghi nhớ (Mnemonics):**
1254|||**💡 Mẹo ghi nhớ (Mnemonics):**
1291|||**💡 Mẹo ghi nhớ (Mnemonics):**
1449|||**💡 Mẹo ghi nhớ (Mnemonics):**
1503|||**💡 Mẹo ghi nhớ (Mnemonics):**
1548|||**💡 Mẹo ghi nhớ (Mnemonics):**
1594|||**💡 Mẹo ghi nhớ (Mnemonics):**
1636|||**💡 Mẹo ghi nhớ (Mnemonics):**
1687|||**💡 Mẹo ghi nhớ (Mnemonics):**
1787|||**💡 Mẹo ghi nhớ (Mnemonics):**
1973|||**💡 Mẹo ghi nhớ (Mnemonics):**
2018|||- 💡 **Mẹo ghi nhớ**: JS là Client, còn lại PPP (PHP, JSP, ASP) đa số là Server.
2031|||- 💡 **Mẹo ghi nhớ**: **T**emporal = **T**ime (Lặp lại nhiều lần/Loop), **S**patial = **S**pace (Gần nhau/Array).
2043|||- 💡 **Mẹo ghi nhớ**: Working Set = Những món đồ đang "Working" (Đang dùng) phải để sẵn trên bàn (Memory).
2056|||- 💡 **Mẹo ghi nhớ**: Kernel là **Lõi** (Hardware), Shell là **Vỏ** (Giao tiếp người dùng).
2071|||- 💡 **Mẹo ghi nhớ**: **R**ecently = Lâu không đụng (Thời gian), **F**requently = Ít dùng (Số lần). 
2083|||- 💡 **Mẹo ghi nhớ**: PCB giống như "Thẻ căn cước + Hồ sơ bệnh án" của một tiến trình.
